# BadmintonSense 接收端后端与前端展示端技术报告

## 一、系统总体架构

BadmintonSense 采用三阶段流水线架构设计，各阶段职责明确、通过 WebSocket 协议解耦通信。

第一阶段 数据发送端 sender.py 负责解析 CSV 传感器数据文件，按 sample_id 分组构建特征矩阵，通过 WebSocket 服务端协议向第二阶段推送原始动作数据。

第二阶段 接收端后端 receiver_backend.py 作为系统核心中枢，承担三项并行职责，其一是作为 WebSocket 客户端连接第一阶段接收原始传感器特征数据，其二是加载预训练深度神经网络模型 best_joint_model.pth 执行在线推理，其三是同时运行 WebSocket 服务端和 HTTP 静态文件服务，分别用于向第三阶段推送推理结果和托管前端静态资源。

第三阶段 前端展示端运行于浏览器环境，通过 WebSocket 客户端连接接收端后端，实时接收并可视化推理结果。

三阶段之间的通信协议均采用 JSON 序列化格式，第一阶段向第二阶段传输包含完整特征矩阵的全量数据报文，第二阶段向第三阶段传输仅包含推理结果和统计信息的精简报文，以降低网络传输开销。


## 二、接收端后端 receiver_backend.py 技术实现

### 2.1 并行服务体系结构

接收端后端采用协作式多任务与多线程混合并行模型。主事件循环基于 Python asyncio 异步框架运行，同时管理两个 WebSocket 端点，其一是连接第一阶段数据发送源的 WebSocket 客户端，其二是面向第三阶段前端的 WebSocket 服务端。HTTP 静态文件服务则运行于独立的后台守护线程，基于 Python http.server 模块的 SimpleHTTPRequestHandler 实现，其 log_message 方法被重写以抑制冗余日志输出。

主协程的执行流程依次为初始化 WebSocket 前端服务端监听端口 8090，然后调用连接发送端的协程函数进入消息处理循环。HTTP 服务器绑定端口 8081 并设置当前工作目录为静态文件根目录，通过 threading.Thread 以 daemon 模式启动。

参数化配置方面，接收端后端支持四个命令行参数，分别是 sender 主机地址及其 WebSocket 端口号、前端 WebSocket 推送端口号、前端 HTTP 服务端口号。各参数均设有合理的默认值，分别对应 localhost 8080 8090 8081。

### 2.2 WebSocket 连接管理机制

与第一阶段发送端的连接采用容错重试策略。connect_to_sender 函数运行于无限循环中，每次迭代尝试建立 WebSocket 连接，连接成功后进入消息迭代循环逐条处理 sender 推送的动作数据。当连接因网络故障或对端关闭而中断时，捕获 ConnectionRefusedError OSError 以及 websockets.exceptions.ConnectionClosed 异常，日志记录错误信息后休眠 5 秒自动重试。

与第三阶段前端的连接采用客户端集合管理策略。全局集合 frontend_clients 存储所有处于连接状态的前端 WebSocket 对象。前端连接处理器 frontend_ws_handler 在新客户端接入时将其加入集合并记录客户端地址和当前在线总数，在连接关闭时将其从集合移除。广播函数 broadcast_to_frontend 使用 asyncio.gather 并发向集合中所有客户端发送 JSON 序列化后的推理结果消息，return_exceptions 参数设为 True 以确保单个客户端的发送异常不会影响其他客户端。

### 2.3 深度神经网络模型架构

接收端后端内置的神经网络模型 JointClassifier 采用双分支异构编码器架构设计，对应两组不同身体部位的传感器配置。这种设计考虑到了不同身体部位运动信号的时序特征差异，上半身传感器信号适宜使用时序卷积网络捕捉局部时序依赖关系，下半身传感器信号适宜使用宽卷积网络捕捉全局空间模式。

#### 2.3.1 TCNEncoder 时序卷积编码器

TCNEncoder 专门处理 Group A 传感器数据，Group A 包含 WTleft 左手腕和 WTwaist 腰部两个传感器，每个传感器采集 X Y Z GX GY GZ 共六个通道数据，因此输入特征维度为 12 通道。

编码器首先通过一个核大小为 1 的 Conv1d 卷积层将输入通道从 12 投影到 32 维隐空间。随后堆叠五个时序卷积块 TemporalBlock，各块的膨胀率 dilation 依次为 1 2 4 8 16 逐层倍增。膨胀卷积的设计使得网络在层数较浅的情况下即可获得较大的时序感受野，底层关注邻近时刻的局部运动模式，高层则能够捕获跨时间步的长程依赖关系。

每个 TemporalBlock 包含两个卷积层序列，每个卷积层后依次连接 Chomp1d 裁剪层、BatchNorm1d 批归一化层、ReLU 激活函数层和 Dropout 随机失活层。其中 Chomp1d 层的设计是实现因果卷积的关键，其作用是裁剪 Conv1d 填充操作引入的右侧多余时间步，确保任意时刻的输出仅依赖于该时刻及之前的输入，不泄漏未来信息。残差连接通过跳跃连接将块的输入与输出相加，缓解梯度消失问题并加速收敛。

时序池化采用基于有效长度的掩码均值池化 Masked Mean Pooling 策略。由于不同动作的持续时长不同，各样本的时间步数 T 存在差异，池化阶段根据序列实际有效长度生成二值掩码矩阵，将填充位置的贡献置零，然后对非填充位置的时序特征取均值，从而将变长的时序特征映射为固定长度的特征向量。最终通过全连接层将 64 维时序特征映射到 256 维瓶颈表示。

#### 2.3.2 WideCNNEncoder 宽卷积编码器

WideCNNEncoder 专门处理 Group B 传感器数据，Group B 包含 FA 左腿前和 EA 右腿后两个传感器，同样每个传感器六个通道，输入特征维度为 12。

编码器堆叠三个宽卷积层，卷积核大小依次为 15 11 7，对应的填充 padding 分别为 7 5 3 以保持时序长度不变。宽卷积核相较于小核卷积能够在单层内覆盖更大范围的时序邻域，适合捕获传感器信号的全局波形模式。每个卷积层后依次连接 BatchNorm1d 批归一化层、ReLU 激活函数层和 MaxPool1d 最大池化层，池化核大小为 2 将时序长度减半。

经过三层池化后时序长度缩减为原始长度的八分之一，使用与 TCNEncoder 相同的掩码均值池化策略将变长特征映射为固定长度向量。最终通过全连接层将 64 维时序特征映射到 128 维瓶颈表示。

#### 2.3.3 分类头 Classifier Head

双分支编码器输出的瓶颈特征在通道维度上拼接，形成 384 维的联合特征表示。分类头采用三层全连接网络结构进行逐级降维和判别特征提取，首先通过 Linear 384 到 256 的变换后接 ReLU 激活和 0.4 失活率的 Dropout 正则化，然后通过 Linear 256 到 128 的变换后接 ReLU 激活和 0.2 失活率的 Dropout，最后通过 Linear 128 到 7 的输出层得到各类别的 logits 分值。Dropout 正则化的渐进递减策略在前两层使用较高的失活率以防止过拟合，在接近输出层时降低失活率以保留更多判别信息。

### 2.4 在线推理流程

推理函数 predict_action 接收 sender 推送的动作数据字典作为输入，其中包含 Group A 特征矩阵 g_a_feat 形状为 T_A 乘以 12、Group B 特征矩阵 g_b_feat 形状为 T_B 乘以 12、以及对应的序列长度信息 g_a_lens 和 g_b_lens。

推理过程首先将 numpy 数组转换为 torch 张量并通过 unsqueeze 操作增加批次维度，然后将张量移至当前计算设备 CPU 或 CUDA。序列长度信息同样转换为长整型张量并移至对应设备。

在 torch no_grad 上下文管理器中执行模型前向传播计算，禁用梯度追踪以降低内存开销并加速推理。模型输出经过 softmax 归一化函数转换为各类别的概率分布，随后使用 torch max 函数沿类别维度取最大值获得预测类别的置信度和索引下标。由于模型输出类别索引从 0 开始而真实标签从 1 开始，推理结果中的预测标签需要执行加一操作以对齐标签空间。

推理完成后，函数在原始动作数据字典的基础上追加 predicted_label、confidence、predicted_label_name 和 label_name 等字段，构成完整的推理结果返回。

### 2.5 控制台输出格式

接收端后端为每个推理结果生成结构化的控制台输出。输出内容采用分隔线包围的分区布局，首先展示动作序号和样本标识符，随后展示输入数据的张量形状，包括 Group A 和 Group B 特征矩阵的行数和列数，接着展示推理结果，包括真实标签及其中文名称、预测标签及其中文名称、置信度的数值和百分比表示，最后根据正确性输出不同标识。

### 2.6 消息协议设计

与第一阶段发送端之间传输的消息采用包含完整特征信息的全量 JSON 报文格式，有效载荷字段包括 sample_id 样本标识符、g_a_feat 和 g_b_feat 分别对应两组的特征矩阵、g_a_lens 和 g_b_lens 分别对应两组的序列长度数组、以及 label 真实类别标签。

与第三阶段前端之间传输的消息采用仅包含推理结果的精简 JSON 报文格式，type 字段标识消息类型为 prediction，sample_id 字段传递样本标识符，label 和 predicted_label 分别传递真实标签和预测标签的数值，label_name 和 predicted_label_name 传递对应的中文名称，confidence 字段传递置信度数值，is_correct 布尔字段指示预测是否正确。


## 三、前端展示端技术实现

### 3.1 技术选型与架构

前端展示端采用零框架纯原生技术栈构建，不依赖 React Vue Angular 等前端框架或任何第三方依赖库。HTML 负责页面结构定义，CSS 负责视觉样式和响应式布局，JavaScript 负责交互逻辑和实时数据通信。这种极简架构选择降低了系统复杂度和部署门槛，用户无需安装任何依赖包仅通过浏览器打开静态文件即可使用。

页面主题为运动感知推理结果展示，核心功能包括通过 WebSocket 协议实时接收接收端后端推送的推理结果、以可视化卡片形式展示当前动作的预测类别和置信度、累计统计正确率和错误数、以及维护按时间倒序排列的历史记录列表。

### 3.2 页面结构与布局

页面采用纵向单列布局，从上到下依次排列五个功能模块。顶部品牌栏 Header 区域展示系统名称 BadmintonSense 和副标题 Motion Analysis，并包含 WebSocket 地址输入框和连接控制按钮。连接状态指示器通过圆点的颜色变化和文字描述实时反映与接收端后端的连接状态。

统计看板 Stats Bar 采用四列等宽网格布局，分别展示已接收动作总数、预测正确数、预测错误数和正确率百分比。该区域为用户提供整体的系统运行概况。

当前推理结果预测卡片 Prediction Card 是页面的核心展示区域。在未接收到任何推理结果时显示等待占位文字。接收到结果后切换为具体内容显示模式，内容包括动作编号和样本标识符、预测标签名称和真实标签名称、正确或错误的状态图标、以及带有颜色渐变的置信度进度条。

推理历史记录列表 History Card 以卡片形式展示过去接收到的所有推理结果，每个历史条目包含动作编号、样本标识符、预测标签和真实标签的中文名称及对应的颜色标识、置信度百分比数值。历史记录采用后进先出的倒序排列方式，最新推理结果置于列表顶部。页面提供清空按钮用于重置历史记录。

### 3.3 CSS 样式设计体系

样式系统采用 CSS 自定义属性定义统一的色彩变量体系。页面背景色使用柔和浅灰色 f1f5f9 以减少视觉疲劳，卡片背景色使用纯白色形成清晰的层次对比。主色调深板岩色 1e293b 用于标题和强调文字，品牌蓝色 3b82f6 用于可交互元素的视觉反馈，成功绿色 10b981 和错误红色 ef4444 分别用于标识预测结果的正确与错误。

布局样式综合运用 CSS Flexbox 和 CSS Grid 两种现代布局方案。统计看板使用 Grid 布局实现等宽四列排列，推理结果卡片内部使用 Flexbox 实现水平居中排列和自适应间距。卡片容器采用较大的圆角半径 16 像素和轻微的盒阴影营造柔和的浮动视觉效果。

交互反馈设计关注微交互动画的细腻度。按钮悬停时向上偏移 1 像素并伴随边框颜色切换产生轻快响应感。置信度进度条的宽度变化通过 CSS transition 属性实现 0.5 秒的平滑过渡动画，使数值变化过程可视化。

响应式适配方面，在视口宽度小于 650 像素的移动端设备上，统计看板从四列自动降为两列排列，推理结果卡片内部的标签和图标从水平并排改为垂直堆叠以防止内容溢出。

### 3.4 JavaScript 核心逻辑

#### 3.4.1 状态管理

前端应用的状态集中存储于 state 单例对象中，该对象包含 WebSocket 实例 socket、布尔型连接状态标识 isConnected、累计动作统计计数器 totalActions correctCount wrongCount、以及按时间倒序存储的历史记录数组 history。这种集中式状态管理方式使得数据流清晰可追踪，各个功能模块通过读写 state 对象实现数据共享和状态同步。

#### 3.4.2 WebSocket 连接管理

连接控制函数 toggleReceiverConnection 通过内联 onclick 方式绑定到页面按钮，采用状态驱动的分支逻辑，当处于已连接状态时执行断开操作关闭 WebSocket 实例，当处于未连接状态时执行连接操作。

连接操作首先从输入框获取 WebSocket 地址并进行非空校验，然后实例化 WebSocket 对象并注册四个生命周期事件处理器。onopen 事件处理器中更新按钮文字和样式为断开模式、更新连接状态指示器、重置预测卡片至占位状态。onmessage 事件处理器是数据入口，从事件对象中提取 JSON 格式的推理结果数据，触发状态更新和 UI 渲染流程。onclose 事件处理器中恢复按钮至初始连接模式、更新连接状态指示器、清空 socket 引用。onerror 事件处理器中通过弹窗提示用户连接失败并提供错误反馈。

#### 3.4.3 推理结果处理流程

每次接收到 type 为 prediction 的 WebSocket 消息时触发完整的处理管线。首先递增累计动作总数计数器，根据 is_correct 字段的值决定递增正确计数还是错误计数，当真实标签缺失即 label 为 N/A 或负一时不更新错误计数以避免统计偏差。然后调用 updateStats 函数更新页面上四个统计数字的文本内容，正确率字段根据正确数除以总数计算百分比。

showPrediction 函数负责更新推理结果卡片的内容和样式。从消息数据中提取预测标签和真实标签的数值及中文名称，根据预测标签的类别编号从预设的颜色映射表中选取对应的十六进制颜色值赋值给标签文字。置信度展示区域将小数形式的置信度乘以 100 转换为百分比格式显示，并根据置信度阈值决定进度条填充颜色，高于百分之八十使用绿色，介于百分之六十和八十之间使用橙色，低于百分之六十使用红色，不同颜色区间为用户提供直观的置信度评估参考。

状态图标根据预测正确性分别显示绿色对勾或红色叉号，并配合浅绿色或浅红色的背景色块增强视觉区分度。当真实标签缺失时显示放大镜图标表示无法判断正确性。

addHistory 函数将当前推理结果构造成包含时间戳唯一标识符的历史条目对象，通过数组的 unshift 方法插入到 history 数组的头部实现倒序排列。renderHistory 函数遍历 history 数组生成每个历史条目的 HTML 结构字符串，通过 innerHTML 一次性写入容器元素。每个历史条目展示动作编号、样本标识符、预测标签和真实标签的中文名称、置信度百分比数值，预测标签文字应用对应的类别颜色标识。

#### 3.4.4 颜色编码方案

七类羽毛球动作各自分配独特的十六进制颜色值用于视觉区分。正手发球使用蓝色 3b82f6、反手发球使用紫色 8b5cf6、正手平抽使用青色 06b6d4、反手平抽使用琥珀色 f59e0b、正手高远球使用翠绿色 10b981、反手高远球使用粉红色 ec4899、杀球使用红色 ef4444。颜色选择兼顾色彩区分度和视觉舒适度，避免使用饱和度过高或相邻色相难以区分的配色方案。

#### 3.4.5 初始化流程

页面 DOMContentLoaded 事件触发后执行初始化函数调用 updateStats 设置统计数字的初始值为零，确保页面加载完成后各数据展示区域处于正确的初始状态。
